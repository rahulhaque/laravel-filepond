services:
  laravel-filepond-8:
    image: laravel-filepond-8-development:8.x
    build:
      context: ./docker/7.3
      dockerfile: Dockerfile
    container_name: laravel-filepond-8-dev
    stdin_open: true
    tty: true
    volumes:
      - '.:/code'
    network_mode: host